name: poost_media_buying_os
description: poost Media Buying OS - complete offline-first Android prototype
publish_to: "none"
version: 1.0.0+4
environment:
  sdk: ">=3.4.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/poost_brand.png
